{
  "name": "governance-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ @synthetixio/governance-subgraph",
    "create-local": "graph create --node http://localhost:8020/ @synthetixio/governance-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ @synthetixio/governance-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 @synthetixio/governance-subgraph",
    "test": "cp -R ../../node_modules/ ./node_modules && graph test -d"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.61.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
