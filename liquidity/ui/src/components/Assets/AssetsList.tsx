import { AssetsTable } from './AssetTable';

// const mockAssets = [{ token: 'ETH', walletBalance: 0.5, walletDollarBalance: 23, value: 1000 }];

export const AssetsList = () => {
  return <AssetsTable />;
};
